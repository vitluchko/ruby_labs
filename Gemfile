# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.2.4'

# gem 'rails'

# Parsing libraries (в алфавітному порядку)
gem 'faraday', '>= 2.0', '< 3.0'
gem 'httparty', '>= 0.22.0'
gem 'nokogiri', '>= 1.15', '< 2.0'
gem 'oj', '>= 3.14', '< 4.0'

# Ruby-RuboCop
gem 'rubocop', require: false

# HTML code styler
gem 'colorize', '>= 0.8', '< 1.0'

# Ruby LSP (Language Server Protocol) для автодоповнення
gem 'solargraph', '>= 0.50', '< 1.0'
